jQuery( document ).ready( ( $ ) => {} )

function abrircurriculum() {
  const myModal = new bootstrap.Modal( document.getElementById( 'doc_cv' ) )
  myModal.show()
}
function abrircatalogo() {
  const myModal = new bootstrap.Modal( document.getElementById( 'doc_cata' ) )
  myModal.show()
}
